/**
 * HomeCarePros — Phone / DID configuration
 * ------------------------------------------------------------
 * This is the ONLY place the phone number needs to be changed.
 * When you receive a real tracking number (DID) from your
 * campaign provider, update the two values below and every
 * phone number + "Call Now" button across the entire site
 * (all pages) will update automatically.
 *
 * PHONE_NUMBER  -> what visitors SEE, formatted for humans
 * PHONE_LINK    -> what the browser DIALS, E.164 format
 * ------------------------------------------------------------
 */
window.SITE_CONFIG = {
  PHONE_NUMBER: "(XXX) XXX-XXXX",
  PHONE_LINK: "tel:+1XXXXXXXXXX"
};
